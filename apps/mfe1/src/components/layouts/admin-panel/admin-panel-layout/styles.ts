import { cva } from "class-variance-authority";

export const adminPanelMainVariants = cva(
  "w-full min-h-[calc(100vh_-_56px)] text-foreground transition-[margin-left] ease-in-out duration-100",
);
